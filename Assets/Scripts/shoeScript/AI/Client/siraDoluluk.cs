using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class siraDoluluk : MonoBehaviour
{
    public bool doluMu;
    // Start is called before the first frame update
    void Start()
    {
        doluMu = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
